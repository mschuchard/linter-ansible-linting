# Include has a linter issue which should trigger and display for the include.
---
- name: Include has issues
  hosts: whatever
  tasks:
    - name: Include with issues
      ansible.builtin.include_tasks: include_with_issues.yml
