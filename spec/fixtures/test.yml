#Edited from ansible-lint/examples.
---
- hosts: webservers
#ANSIBLE0008
- sudo_user: root

  tasks:
  - name: ANSIBLE0004
    action: git version=HEAD c=d

  - name: ANSIBLE0006
    action: command chdir=bobbins creates=whatever /usr/bin/git clone blah

  - name: ANSIBLE0005 and ANSIBLE0012
    action: command hg clone foo

  #ANSIBLE0011 and ANSIBLE0013
  - shell: echo hello creates=/foo

  - name: ANSIBLE0007 and ANSIBLE0014
    command: /bin/mkdir /path/to/dir creates=/path/to/dir HOME=/foo

  - name: ANSIBLE0009
    file: path=/path/to/file mode=123

  - name: ANSIBLE0010 and ANSIBLE0015
    yum: name=ansible state=latest
    with_items: items
